node_modules/
.env
.env.local
.vercel
.claude/
# force deploy

.vercel
.env*.local

# Local demo seeding: hardcodes a password and resets a real account.
# This repo is public, so it must never be committed.
seed-dailydose.js
